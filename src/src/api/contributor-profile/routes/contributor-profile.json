{
  "routes": [
    {
      "method": "GET",
      "path": "/contributor-profiles",
      "handler": "contributor-profile.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/contributor-profiles/:id",
      "handler": "contributor-profile.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/contributor-profiles",
      "handler": "contributor-profile.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/contributor-profiles/:id",
      "handler": "contributor-profile.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/contributor-profiles/:id",
      "handler": "contributor-profile.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
